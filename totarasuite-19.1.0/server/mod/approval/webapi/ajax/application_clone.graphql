mutation mod_approval_application_clone(
  $input: mod_approval_application_input!
) {
  mod_approval_application_clone(input: $input) {
    application {
      id
      page_urls {
        edit
      }
    }
  }
}
