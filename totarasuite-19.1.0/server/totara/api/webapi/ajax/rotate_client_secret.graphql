mutation totara_api_rotate_client_secret($id: core_id!) {
  result: totara_api_rotate_client_secret(id: $id) {
    oauth2_client_providers {
      id
      client_id
      client_secret
    }
    active_tokens
  }
}
