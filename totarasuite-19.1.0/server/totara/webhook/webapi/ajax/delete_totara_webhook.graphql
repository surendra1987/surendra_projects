mutation totara_webhook_delete_totara_webhook(
  $reference: totara_webhook_totara_webhook_reference!
) {
  totara_webhook: totara_webhook_delete_totara_webhook(reference: $reference) {
    deleted_id
  }
}
